<div class="flex items-center justify-between flex-shrink-0 px-3 mb-4">
    <!-- Logo -->
    <a href="{{ route('admin.dashboard') }}" class="text-xl font-semibold">
        <img class="w-14 h-auto" src="{{ asset('images/logo-white.svg') }}" alt="{{ config('app.name', 'RestoPos') }}">
        <span class="sr-only">{{ config('app.name', 'RestoPos') }}</span>
    </a>
</div>
